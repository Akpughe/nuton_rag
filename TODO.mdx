-- ADD STUDY Guide for each course -- it has to be an indepth study guide -- I will drop some samples: /Users/davak/Downloads/study-guide-ck0t8i.txt, /Users/davak/Downloads/study-guide-wt2k93.txt
we need to generate a very good prompt that covers all these -- find the right skill that can do this
-- Generate Course Final Exam (MCQ, Fill in the gap and Theory) -- let's the best way to do that -- find the right skill that can help use generate the best prompt for this
also ensure that we cover 90% of the sources that we either get from pinecone via RAG or just from the content/topic
-- Increase the speed at which we process documents in the from-files endpoint -- we need to process multiple files/links/youtube_videos in parallel
-- work in the course ask feature -- we need to store chats in the DB too -- and we need context history for the chat-- so that it know previous messages - we can use redis here but also fall back to the DB if redis fails
-- add flashcard to the courses right now we have only quiz. let's the best way to do that -- find the right skill that can help use generate the best prompt for this
also ensure that we cover 90% of the sources that we either get from pinecone via RAG or just from the content/topic
--

-- Add restrictions for free users to only generate chapter 1
